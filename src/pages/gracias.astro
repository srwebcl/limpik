---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const title = "Gracias | Limpik";
const description = "Mensaje recibido. Nos pondremos en contacto pronto.";
---

<Layout title={title} description={description}>
    <Header />

    <main class="main-content-spaced">
        <section class="section-thank-you">
            <div class="container">
                <div class="content-box">
                    <div class="icon-success">
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="64"
                            height="64"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            class="feather feather-check-circle"
                        >
                            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                            <polyline points="22 4 12 14.01 9 11.01"></polyline>
                        </svg>
                    </div>
                    <h1>¡Gracias por contactarnos!</h1>
                    <p>
                        Hemos recibido su mensaje correctamente. Nuestro equipo
                        revisará su solicitud y le contactará a la brevedad
                        posible, usualmente dentro de las próximas 24 horas
                        hábiles.
                    </p>
                    <div class="actions">
                        <a href="/" class="btn-home">Volver al Inicio</a>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <Footer />
</Layout>

<style>
    .section-thank-you {
        min-height: 60vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8fafc;
        padding: 4rem 0;
    }

    .content-box {
        background: white;
        padding: 4rem 2rem;
        border-radius: 20px;
        box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 600px;
        margin: 0 auto;
        border: 1px solid var(--color-border);
    }

    .icon-success {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 80px;
        height: 80px;
        background-color: rgba(34, 197, 94, 0.1);
        border-radius: 50%;
        color: #22c55e;
        margin-bottom: 2rem;
    }

    h1 {
        font-size: 2.5rem;
        color: var(--color-primary);
        font-weight: 800;
        margin-bottom: 1.5rem;
        line-height: 1.2;
    }

    p {
        color: var(--color-text-main);
        font-size: 1.1rem;
        line-height: 1.6;
        margin-bottom: 3rem;
    }

    .btn-home {
        display: inline-block;
        background-color: var(--color-secondary);
        color: white;
        padding: 1rem 2.5rem;
        border-radius: 50px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 4px 6px -1px rgba(249, 115, 22, 0.3);
    }

    .btn-home:hover {
        background-color: #ea580c;
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(249, 115, 22, 0.4);
    }

    @media (max-width: 600px) {
        .content-box {
            padding: 3rem 1.5rem;
            margin: 0 1rem;
        }
        h1 {
            font-size: 2rem;
        }
    }
</style>
